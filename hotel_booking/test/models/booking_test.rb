require 'test_helper'

class BookingTest < ActiveSupport::TestCase
  
  # test "Check In and Check Out date are required validators" do
  #   hotel = Hotel.first
  #   room_type = hotel.room_types.first

  #   booking = Booking.new
  #   booking.hotel_id = hotel.id
  #   booking.room_type = room_type.id
  #   booking.amount = room_type.base_rate * 30

  #   assert_not booking.valid?
  #   assert_equal "can't be blank", booking.errors.messages[:check_in_date]
  # end
end
